{
  "name": "mid-fekar",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "bot:build": "pnpm --filter bot build",
    "bot:start": "pnpm --filter bot start",
    "bot:dev": "pnpm --filter bot dev",
    "deploy-commands:build": "pnpm --filter deploy-commands build",
    "deploy-commands:start": "pnpm --filter deploy-commands start",
    "shared-commands:build": "pnpm --filter '~shared-commands' build",
    "shared-commands:start": "pnpm --filter '~shared-commands' start"
  },
  "keywords": [
    "discord-bot",
    "typescript",
    "fp-ts"
  ],
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.1.1",
    "prettier": "^3.1.0",
    "@total-typescript/ts-reset": "^0.5.1",
    "typescript": "^5.3.2"
  },
  "author": "cweiqi27",
  "license": "MIT"
}
